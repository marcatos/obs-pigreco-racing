# Workstream: Engagement

**Roadmap IDs:** P2-01 … P2-05  
**Owns (planned):** `adapters/streamelements/`, `adapters/streamdeck/`, docs for replay.

## Goals

Viewer interaction chrome matches PiGreco theme; streamer controls are one-click.

## Tasks

- P2-01 Chat CSS theme (export + install notes for SE / own browser chat)
- P2-02 Alert widgets theme
- P2-03 Hotkey cheat-sheet + Stream Deck / VirtualDeck profile JSON — **done** (`docs/OBS_VIRTUALDECK.md`, `adapters/streamdeck/`)
- P2-04 Highlight scene wired to replay buffer — **done** (Instant Replay 960×540 on Live/Headcam via Session Director + OBS Replay Buffer)
- P2-05 YouTube like / subscribe / bell promo — **done** (`overlays/youtube-promo.js`)

## Constraints

Must use DESIGN_SYSTEM colors; no second brand. Keep files optional so core pack works offline without SE account.
