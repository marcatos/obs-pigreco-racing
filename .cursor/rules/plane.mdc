---
description: Plane is the canonical tracker for obs-pigreco-racing
alwaysApply: true
---

# Plane tracker — OBS PiGreco racing

Workspace `personal`. This folder is Plane project **OBSPI** (`obs-pigreco-racing`).
LAN `http://10.100.30.87`. UI `https://plane.marcatos.ddns.net`.

## Canonical owners

- Live facts → Plane work item named **STATUS** (Community v1.4.1 MCP `page` is 404)
- Decisions → Plane work item named **DECISIONS** (append at top of that issue)
- Work → Plane work items (labels feat/fix/chore/known-issue/backlog)
- Session notes → comment on the work item (do not add git handoffs)
- Changelog → conventional commits
- Code → git

List open work items for `OBSPI` at session start. Create a work item before coding.
If MCP create fails (`title` vs `name`), run:

```powershell
python C:\Users\simot\Documents\Projects\opnsense-server\scripts\plane_issue.py create --project OBSPI --title "..."
```

Do not use Linear or GitHub Issues. Do not put secrets on Plane. Do not treat git STATUS/TODO/ROADMAP/DECISIONS as live.
