# iRacing track SVG cache (P3-07)

Official map layers downloaded by `Start-SyncTrackMaps.bat` / `sync_iracing_track_maps.py`.

- Files `*.svg` and `*.meta.json` are **gitignored** (iRacing assets — personal local use).
- Overlay looks up `assets/tracks/iracing/{TrackID}.svg` (e.g. `449.svg`).

Do not commit downloaded SVGs into the public pack.
